#include <stdio.h>
 main()
 {
    int x,y;
    printf("The two number:");
    scanf("%d%d",x,y);
    if(x>y)
    {
        printf("greatest number is%d",x);
    }
    else
    {
        printf("greatest number is %d",y);
    }
    return 0;
 }

